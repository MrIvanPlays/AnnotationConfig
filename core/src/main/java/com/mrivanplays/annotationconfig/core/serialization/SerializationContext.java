package com.mrivanplays.annotationconfig.core.serialization;

/**
 * Represents a serialization context. This holds information about the {@link
 * java.lang.reflect.Field} that's being (de)serialized, but since AnnotationConfig can not always
 * get a {@link java.lang.reflect.Field} instance, this context class will hold information.
 *
 * @param <T> generic type of information that's being (de)serialized
 * @author MrIvanPlays
 * @since 3.0.0
 */
public final class SerializationContext<T> {

  // todo

  /*
  private final Optional<String> name;
  private final T def;
  private final Class<?> classType;
  private final Type genericType;
  private final Type genericSuperclass;
   */

}
